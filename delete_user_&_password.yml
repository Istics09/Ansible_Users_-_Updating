---
- name: Delete user
  hosts: all
  become: yes

  vars:
    username: Istics
  
  tasks: 
    - name: "Remove user"
      user:
        name: '{{ username }}'
        state: absent
        remove: yes